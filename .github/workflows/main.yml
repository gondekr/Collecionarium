name: CI
on: [push]
jobs:
  build:
    name: First Job
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: make install
    - name: Run tests
      run: make test_xcov
